[wokwi]
version = 1
# Only needed if you use the Wokwi CLI or "Wokwi for VS Code" extension to run
# this sketch/diagram pair directly — the plain web UI at wokwi.com (see
# WOKWI_SETUP.md) doesn't read this file at all.
#
# No rfc2217ServerPort here: that was for relaying a live Wokwi simulation's
# serial port to FORMFIND over WebSocket (wokwi_ws_bridge.py), which has been
# retired in favor of avr8js_sim_bridge.js — see AVR8JS_SETUP.md for the
# live-link path now used instead.
